# 48 — สถานะโปรเจกต์ ณ 7 กันยายน 2569

ฉบับภาษาไทยของสถานะปัจจุบัน อ่านจาก repo และผลรันจริงในวันเดียวกัน ต่อจาก
`32-status-2026-09-02.md` (สถานะ 2 ก.ย. และอัปเดต 5 ก.ย.) ฉบับภาษาอังกฤษแบบสั้น
อยู่ด้านบนของ `01-build-status.md` หน้าเว็บของสถานะนี้อยู่ที่
https://claude.ai/code/artifact/0296700e-bc21-4ce8-9d0c-6d3ec412d441

## ภาพรวม

| | |
|---|---|
| โค้ด | github.com/domexxzz/chivago · 123 commits (58 ตั้งแต่สถานะ 2 ก.ย.) · `main` สะอาดที่ `39c9ee2` |
| Demo | https://chivago-demo.vercel.app · bundle `index-bcc9cd22` (build จาก `main` หลังรวม PR #20) · static build จาก snapshot ของ API จริงหลัง `demo:reset` วันนี้ · `chivago-demo-sigma.vercel.app` (บัญชี electiction, ที่ README ชี้) ยังเป็น bundle `index-8c4ecd0c` ก่อนงานวันนี้ทั้งหมด |
| API จริง | `chivago.fly.dev` ยังไม่ขึ้น (health ไม่ตอบ ตรวจ 7 ก.ย. ค่ำ) · เครื่องนี้ไม่มีบัญชี Fly · เป็นเส้นตายที่แข็งที่สุดของสัปดาห์: อังคาร 8 ก.ย. 20:00 (docs/47 ข้อ 0) |
| Tests | 1,571 TypeScript (core 360 · api 784 · mobile 377 + 25 · tokens 25) + Dart 11 + Swift 10 · typecheck ผ่านทั้ง 4 แพ็กเกจ (นับหลังรวม PR #20) |
| ขนาด | 20 หน้าจอ · 75 API routes · 32 console routes · 38 ตาราง · ซอร์สประมาณ 43,200 บรรทัด / เทสต์ 19,300 · เอกสาร 49 ฉบับ |
| CI | เขียวทั้งสามงาน (typescript · dart · swift) ทุก commit ของวันนี้ รวมถึง PR #20 หลัง rebase |
| PR #20 | ของ electiction "before the room fills" rebase บน main วันนี้ เอกสารเป็นเลข 49 แล้วรวมเป็น `b8bf790` ค่ำวันที่ 7 ดูหัวข้อท้าย |

## สรุปห้าบรรทัด

- **แผน KU Sriracha สี่วัน (docs/42) สร้างครบก่อนกำหนด:** แคมปัสเป็นพื้นที่ที่สองพร้อมค่าอากาศภาคพื้นดินแห่งแรก (43) · สตอรี่บนหมุด (44) · หน้าจอสตอรี่และ board บนโปรเจกเตอร์ (45) · โหมดงาน: ประตู token limit และประกาศ PDPA (46) ทั้งหมดอยู่บน `main` และในเดโมสาธารณะ
- **วันนี้เพิ่มฝั่งผู้ใช้สามชิ้น:** หน้าโปรไฟล์ที่ไม่แต่งชื่อหรือรูป (อันดับจริงจากงานที่ตรวจแล้ว) · เหรียญตรา 7 อันจากการไปสถานที่ (นับเฉพาะเช็กอินในรั้ว) · นักเดินทางคนที่สองใน seed เดโม ทำให้อันดับเป็นอันดับจริง (#1 จาก 2)
- **บั๊กจากสกรีนช็อตเมื่อเช้า** ("KU Sriracha" บนแผนที่สมุย) เกิดจากแผนที่ถูกสร้างครั้งเดียวต่อการเปิดหน้า แก้และ deploy แล้ว (`3a75d2d`)
- **เช็กลิสต์ซ้อมวันพุธ 9 ก.ย.** เขียนแล้ว (docs/47 และหน้าเว็บติ๊กได้) พร้อมขั้นตอน reset และ link code ให้เครื่องบนเวทีเป็น demo-user ซึ่งอยู่ใน runbook docs/46 ด้วย
- **ที่ยังไม่มีคือของที่ต้องการทีมหรือคนนอก** ไม่ใช่โค้ด: API บน Fly · รูป 5 สถานที่ · นักกฎหมายอ่านประกาศ · เลือก URL เดโมอันเดียว

## ความคืบหน้าตามคำแนะนำ 7 ข้อจากรายงานคู่แข่ง

| # | คำแนะนำ | สถานะ | อยู่ที่ไหน |
|---|---|---|---|
| 01 | เตรียมพาร์ทเนอร์ Trash Hero Koh Samui | เตรียมแล้ว | `partners/trash-hero-koh-samui.md` · ยังไม่ seed จนกว่าเขาจะตกลง · ยังไม่ได้ส่งข้อเสนอ |
| 02 | Recorded, not scored | เสร็จ | docs/29 · วันนี้ยืนยันซ้ำว่าแสตมป์ที่บันทึกเองไม่นับเป็นเหรียญ (medals.ts) |
| 03 | สัญญาณที่สองบน geofence | เสร็จ | docs/30 · ใช้กับสตอรี่ (docs/44) และเหรียญด้วย |
| 04 | คำขาย "ชั้นหลักฐานที่โรงแรมยังไม่มี" | เสร็จ | docs/31 · statement เป็น CSV/PDF แล้ว (docs/39) |
| 05 | ปิดคำถามกฎหมายไทยก่อนเปิดตัว | ยังไม่เริ่ม | เพิ่มประเด็นใหม่: ประกาศก่อนถ่ายสตอรี่ (docs/46) ยังไม่มีนักกฎหมายอ่าน |
| 06 | ไม่เปิดเศรษฐกิจแต้มก่อนสปอนเซอร์เซ็น | ถือปฏิบัติโดยปริยาย | ไม่เปลี่ยน |
| 07 | เปิด 77 จังหวัดแบบ Roadside | บางส่วน | mascot 77 ตัวมีแล้ว (docs/33, 35 ของ electiction) · สถานที่จริงนอกสมุยและศรีราชายังไม่มี · ชลบุรี (TH-20) เป็นจังหวัดที่สองที่มีสถานที่ |

## สิ่งที่ส่ง 5–7 ก.ย. จากเครื่องนี้

| commit | เรื่อง |
|---|---|
| `a0a7517` | วันที่ 1 แคมปัส: จังหวัดที่สอง 5 สถานที่จาก OSM host ทีม สถานี Air4Thai o61 เป็นค่าอากาศภาคพื้นดินแห่งแรก (bundle TLS chain ที่เซิร์ฟเวอร์ส่งมาผิด) ชิปสลับพื้นที่ ตึก 3 มิติ |
| `25981cb` | วันที่ 2 สตอรี่: ตาราง อัปโหลด sniff container ffmpeg → H.264 720p + poster รั้ว 250 ม. + สัญญาณที่สอง 3 คลิป/วัน หมดอายุ 7 วัน host ตรวจเฉพาะที่ตัวเอง host |
| `af9887d` | วันที่ 3 หน้าจอ: วงแหวนทองบนหมุด viewer แนวตั้ง ประตู board บนโปรเจกเตอร์ poll ทุก 3 วิ deep link `?place=` |
| `09386d9` | วันที่ 4 โหมดงาน: ประตูจากคอนโซล (ตาราง settings) token ใน QR limit ลงทะเบียน 500/ชม. ประกาศสองภาษา สคริปต์ QR Fly config และ ffmpeg ใน Dockerfile |
| `3a75d2d` | แผนที่ตามชิปพื้นที่: `key={area.key}` สร้างแผนที่ใหม่เมื่อเปลี่ยนพื้นที่ · ข้อความว่างแยก "ไม่มีสถานที่ในพื้นที่นี้" จาก "ไม่ตรงตัวกรอง" |
| `677c6b6` · `0907da8` | เช็กลิสต์ซ้อม 9 ก.ย. (docs/47) และหน้าเว็บติ๊กได้ |
| `c97b62b` | โปรไฟล์: ชื่อจากบัญชี รูปจาก companion ที่โตที่สุด เลเวล/EXP เส้นทางระดับ อันดับ (มีเฉพาะเมื่อผู้ร่วม ≥ 2 พร้อมเกณฑ์) · `/standing.position` · ไอคอนมุมขวาบน Home · แก้ `demo:reset` ที่พังตั้งแต่มีตาราง settings |
| `8bee27f` | เหรียญตรา 7 อัน (core `medals.ts` กฎเป็นข้อมูล) · `GET /medals` · กริดบนโปรไฟล์ หน้าเหรียญ toast ตอนเช็กอิน · เดโมนับจริงจากเช็กอินของตัวเอง |
| `40509ac` | นักเดินทางคนที่สองใน seed เดโม ผ่านเซอร์วิสจริง (เช็กอิน 2 วัน + ภารกิจที่เทศบาลอนุมัติ) · walk check "Standing #1 of 2" |
| `39c9ee2` | docs/46 รู้จักขั้นตอน reset และ link code สำหรับเครื่องบนเวที |

## ตรวจสอบจริงแล้ววันนี้ (ไม่ใช่แค่เทสต์)

- **แผนที่:** ใน Chrome จริงบน production สลับชิปสมุย → มก. → สมุย แผนที่เปลี่ยนตามทั้งสองทาง (ก่อนแก้: ค้างที่พื้นที่แรก)
- **โปรไฟล์และเหรียญ:** Chrome จริงบน build ที่ deploy ทั้งอังกฤษและไทย · หน้าเหรียญมีวันที่ได้ตามเช็กอินจริง (3–5 ก.ย.)
- **เดโมนับเหรียญจริง:** ยิงเช็กอินหอสมุดผ่านเซิร์ฟเวอร์เดโมในบันเดิลจริง → 5/7 กลายเป็น 6/7 ได้ "ข้ามพื้นที่" และ "ทั่วแคมปัส" ขยับ 1/5
- **อันดับ:** `demo:reset --walk` ผ่าน 10 จอ รวม `Standing #1 of 2` · `/standing` จาก API จริงตอบ participants 2 · position 1 · โปรไฟล์บน production แสดง "#1 of 2 participants"
- **CI:** เขียวทุก commit วันนี้ (typescript · dart · swift)

## สิ่งที่ยังไม่จริง

- **API จริงยังไม่ขึ้น Fly** ทุกอย่างของวันงาน (QR สตอรี่ ประตู board) ต้องการมัน เดโมสาธารณะทำแทนไม่ได้เพราะไม่รับอัปโหลด
- **คลิปที่ approve แล้วเอาลงจากคอนโซลไม่ได้** คอนโซลแสดงเฉพาะคิวรอตรวจ (PR #20 ที่รวมแล้วทำให้ hide มีผลบน board ทันที แต่ยังไม่มีปุ่มสำหรับคลิปที่ approve แล้ว) ทางออกชั่วคราวใน docs/47: ดูให้จบก่อนกด Approve
- **ยังคงเดิมจาก docs/32:** เงินไม่เคลื่อน · 75 จังหวัดไม่มีสถานที่ · ไม่มี SMS และไม่มีใครเฝ้า SOS desk · อากาศสมุยเป็นค่าประมาณ 11 km (แคมปัสมีสถานีจริงแล้ว) · ไทยยังไม่ผ่านเจ้าของภาษา · ไม่เคยรันบนเครื่องจริง (สตอรี่ต้องใช้กล้องจริงในวันพุธ) · ไม่มี device attestation · ลายเซ็นอิสระบน statement
- **เหรียญเป็นสัญลักษณ์วาดตามระบบดีไซน์** ไม่ใช่ภาพประกอบอย่างในตัวอย่างที่ส่งมา ต้องมีไฟล์ภาพจากทีมถ้าจะเปลี่ยน
- **มีเดโมสองอัน** README ชี้ sigma ซึ่งล้าหลัง 8 commits ของวันนี้ ต้องเลือกอันเดียว

## PR #20 ของ electiction รวมแล้ว

"before the room fills" (เปิด 6 ก.ย. 19:27 UTC · +536/−42): เพดาน 25 MB
ตรวจจาก content-length ก่อนอ่าน body (413) · iPhone HEIC แยกจาก MP4 ไปทางรูป
พร้อมคำปฏิเสธที่บอกให้แก้ Settings · media ของ board เป็น `no-cache` + ETag ทำให้ hide
มีผลทันที · ทั้งสี่ข้อมีเทสต์ที่ล้มบนโค้ดเก่า เขียนไว้ใน `49-before-the-room-fills.md`

รวมค่ำวันที่ 7 เป็น `b8bf790` หลัง rebase บน `main` ของวัน (conflict มีจุดเดียวคือ
บรรทัดนับเทสต์ใน README) เอกสารในนั้นเปลี่ยนเลขจาก 47 เป็น 49 ทุกชุดเทสต์ผ่านบน tree
ที่รวมแล้วก่อน push และ CI ของ PR เขียวทั้งสามงาน เดโมสาธารณะ build ใหม่จาก `main`
หลังรวม

## ปฏิทินสัปดาห์นี้

| วัน | งาน | เกณฑ์ผ่าน |
|---|---|---|
| อังคาร 8 | ทีม: `fly launch` · seed · `demo:reset --walk` · link code ให้เครื่องเวที · secrets · QR (docs/46 ข้อ 1–6) | `chivago.fly.dev/health` ตอบ 200 ก่อน 20:00 |
| พุธ 9 | ซ้อมที่แคมปัสตาม docs/47: เน็ต 5 เครื่อง · flow 3 รอบติด · drills · วิดีโอสำรอง · คลิปและรูปของทีม | ผ่าน 3 รอบติดบนเน็ตจริง |
| พฤหัส 10 | freeze เที่ยง · พิมพ์ QR ตัวจริง · ซ้อมสั้น | ไม่แก้อะไรหลังเที่ยง |
| ศุกร์ 11 | pitch · ฮุก 90 วิ · ประตูเปิด-ปิดจากเวที | — |

## ค้างส่ง เรียงตามที่ควรทำก่อน

1. **ทีม (ไม่ใช่โค้ด):** deploy Fly + seed + reset + link code ภายในอังคารเย็น · รูป 5 สถานที่และคลิปทีม 3–5 ในวันพุธ · วิดีโอสำรอง
2. **ปุ่มเอาคลิปที่ approve แล้วลง** ในคอนโซล (รายการ "กำลังแสดง" + ปุ่ม + เทสต์ ประมาณครึ่งวัน)
3. **เจ้าของตัดสินใจ:** URL เดโมอันเดียว · นักกฎหมายอ่านประกาศและข้อ 05 · ส่งข้อเสนอ Trash Hero · สปอนเซอร์ · เซนเซอร์ PM2.5 · ใครเฝ้า SOS desk
4. **จากเดิม:** ข้อ 07 สถานที่นอกสองพื้นที่ · native Thai review · รันบนเครื่องจริง (วันพุธเป็นครั้งแรก)

ตัวเลขทุกตัวในหน้านี้อ่านจาก repo และผลรันจริง ณ 7 ก.ย. 2569
